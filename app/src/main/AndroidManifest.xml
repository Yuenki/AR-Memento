<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ar_memento">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <!--
     The top xmlns:android and xmlns:tools seem to enable the android:
     and tools: below.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!--
    Indicates that the app requires ARCore ("AR Required"). Ensures the app is
    visible only in the Google Play Store on devices that support ARCore.
    -->
    <!-- Sceneform requires OpenGL ES 3.0 or later. -->
    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="true" />
    <uses-feature android:name="android.hardware.camera.ar" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".ImagePickActivity"
            android:label="ImagePick"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".SettingsActivity"
            android:label="Settings"
            android:parentActivityName=".MainActivity"/>

<!--        Activity that recognizes images. The symbol portion.-->
<!--        This should be used as a LAST RESORT to handle config changes.-->
<!--        android:configChanges="orientation|screenSize"-->
<!--        This^ does not restart the activity due to config changes-->
        <activity
            android:name=".ScannerActivity"
            android:label="ScannerActivity"
            android:screenOrientation="locked"
            android:parentActivityName=".MainActivity">
        </activity>

        <activity
            android:name=".CameraActivity"
            android:label="@string/title_activity_camera"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.ar_memento.MainActivity" />

        </activity>
        <!--
         Indicates that the app requires ARCore ("AR Required"). Causes the
         Google Play Store to download and install Google Play Services for AR
         when the app is installed.
        -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="required" /> <!-- The .MainActivity is the first screen the user sees. -->
        <!--
        An activity represents a single screen with a user interface.
        Although the activities work together to form a cohesive user
        experience in the app,each one is independent of the others
        -->
        <activity android:name=".MainActivity" android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>