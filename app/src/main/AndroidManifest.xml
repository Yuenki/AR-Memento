<?xml version="1.0" encoding="utf-8"?>
<!--
Fernando Herrera
Started 10.25.019
-->
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ar_memento">
    <!--The top xmlns:android and xmlns:tools seem to enable the android: and tools: below.-->

    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Indicates that the app requires ARCore ("AR Required"). Ensures the app is
         visible only in the Google Play Store on devices that support ARCore.
    -->
    <uses-feature android:name="android.hardware.camera.ar" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <!-- Indicates that the app requires ARCore ("AR Required"). Causes the Google
        Play Store to download and install Google Play Services for AR when the
        app is installed.
        -->
        <meta-data android:name="com.google.ar.core" android:value="required" />
        <!-- The .MainActivity is conventionally the first screen the user sees.-->
        <!-- An activity represents a single screen with a user interface.
        Although the activities work together to form a cohesive user experience in the app,
        each one is independent of the others
        -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
